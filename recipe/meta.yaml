{% set name = "asp-plot" %}
{% set version = "1.9.0" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/asp_plot-{{ version }}.tar.gz
  sha256: 1eb6ef17a5f72675361436791153a4c3d3e25ef4e0059ff867ac5ff624c4d50d

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - asp_plot = asp_plot.cli.asp_plot:main
    - csm_camera_plot = asp_plot.cli.csm_camera_plot:main
    - stereo_geom = asp_plot.cli.stereo_geom:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}
    - click
    - contextily
    - fpdf2
    - gdal
    - geopandas
    - matplotlib-base
    - matplotlib-scalebar
    - numpy
    - pandas
    - pyproj
    - rasterio
    - rioxarray
    - scipy
    - shapely
    - sliderule
    - xarray

test:
  imports:
    - asp_plot
  commands:
    - asp_plot --help
    - csm_camera_plot --help
    - stereo_geom --help

about:
  home: https://github.com/uw-cryo/asp_plot
  summary: Package for plotting outputs of Ames Stereo Pipeline processing
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/uw-cryo/asp_plot

extra:
  recipe-maintainers:
    - bpurinton
