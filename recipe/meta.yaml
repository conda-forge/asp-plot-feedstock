{% set name = "asp-plot" %}
{% set version = "1.14.1" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/asp_plot-{{ version }}.tar.gz
  sha256: 4098caa87e5c4bb39f7b04e7eff93597bce368cf1d8ae509ab0c3f4e96582500

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - asp_plot = asp_plot.cli.asp_plot:main
    - csm_camera_plot = asp_plot.cli.csm_camera_plot:main
    - stereo_geom = asp_plot.cli.stereo_geom:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}
    - click
    - contextily
    - fpdf2
    - gdal
    - geopandas
    - matplotlib-base
    - matplotlib-scalebar
    - numpy
    - pandas
    - pyproj
    - rasterio
    - rioxarray
    - scipy
    - shapely
    - sliderule
    - xarray

test:
  imports:
    - asp_plot
  commands:
    - asp_plot --help
    - csm_camera_plot --help
    - stereo_geom --help

about:
  home: https://github.com/uw-cryo/asp_plot
  summary: Package for plotting outputs of Ames Stereo Pipeline processing
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/uw-cryo/asp_plot

extra:
  recipe-maintainers:
    - bpurinton
